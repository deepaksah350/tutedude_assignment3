 task1 ::Factorial (Recursive): Calculates and prints the factorial of a number using a recursive function.
   task2::Computes and displays the square root, natural log, and sine of a number using the math module.

